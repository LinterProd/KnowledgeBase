meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "metarash",
    "password": "12345"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}

settings {
  encodeUrl: true
  timeout: 0
}
