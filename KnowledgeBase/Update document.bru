meta {
  name: Update document
  type: http
  seq: 9
}

put {
  url: {{url}}/api/documents/1
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  title: legend.pdf
}

settings {
  encodeUrl: true
  timeout: 0
}
